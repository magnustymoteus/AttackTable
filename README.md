# Attack Table
## TODO
- Currently the table is for white attacks (auto-attacks) only, we need to include yellow attacks as well.
- A "attack table calculator" that lets you pick an ability and list of (de)buffs and get the result instead of only real-time.
- Take (de)buffs into account
- Attack table for PvP
- Ability to record combat and aggregate the outcomes in order to compare it with the table

